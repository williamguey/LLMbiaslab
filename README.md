# LLMbiaslab

Gradio app for generating mirrored probes and running robustness tests across multiple LLMs using OpenRouter.

## Setup

1. Create and activate a virtual environment
2. Install dependencies

```bash
pip install -r requirements.txt
